{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for OCR operator extraction from shift photos. Obtain from Google AI Studio (aistudio.google.com). Optional — app has a text-input fallback when the key is absent.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    }
  ]
}
